"use client";

export default function EntityViewerError() {
    return (
        <div className="mx-auto max-w-6xl px-6 py-24 text-center text-sm text-zinc-500">
            Entity data is temporarily unavailable - try again shortly.
        </div>
    );
}
